# Archive

This directory keeps historical design notes, troubleshooting write-ups, and
stage summaries that are still useful for reference but no longer belong in the
repository root.

Current documentation entry points remain in the root `README.md`, `api/`,
`config/`, `docs/`, and `services/path_selection/README.md`.
