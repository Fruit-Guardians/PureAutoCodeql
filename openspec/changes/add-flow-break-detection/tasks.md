## 1. Implementation
- [ ] 1.1 现状调研：梳理 `CodeQLComposeTool` 迭代流程与 `CodeQLExecutionService` 空结果处理路径，明确插入断流检测的最佳钩子及输入输出契约
- [ ] 1.2 接口设计：定义“断流检测 + 补边生成”在 Compose 层的调度接口（包含配置项、超时、返回结构、遥测事件）
- [ ] 1.3 查询抽取器：从上一轮 QL 文本解析出 Source/Sink 及既有 `isAdditionalFlowStep`（支持多种写法、注释/换行处理）
- [ ] 1.4 骨架构建器：将抽取结果与 `<SOURCE>/<SINK>/<ISADDITION>` 占位替换到断流检测骨架，处理语言差异与缺失谓词的回退方案
- [ ] 1.5 检测执行器：调用 CodeQL CLI/Runner 执行检测查询，解析取得的断流点（文件、起止位置、节点语义标签）
- [ ] 1.6 断流点归类：结合源语言 AST/LSP 信息（若可用）对候选断流点做类型归类与优先级排序，过滤低价值噪声
- [ ] 1.7 补边生成器：基于断流点生成 `isAdditionalFlowStep(src,dst)` 子句，覆盖赋值、属性、集合、调用/返回等规则，并支持自定义扩展
- [ ] 1.8 子句去重与限额：实现跨轮次 OR 子句合并、去重、上限控制及自环/跨文件不合理连接筛除
- [ ] 1.9 查询合并器：将新增子句安全地插入原查询（保持格式、缩进、注释），并确保不存在覆盖原逻辑的问题
- [ ] 1.10 迭代编排：在 Compose 流程中实现“检测→补边→重跑”的循环，接入可配置的轮次/耗时上限与降级逻辑
- [ ] 1.11 遥测与日志：输出断流点候选、生成子句、迭代次数、耗时等信息，便于调试与后续优化
- [ ] 1.12 配置与开关：新增 `enableFlowBreakDetection` 等配置项，默认值、动态调整入口与向后兼容处理
- [ ] 1.13 回退策略：检测/执行/合并异常时，保证快速回退到现有空结果重试流程，不影响原有成功路径
- [ ] 1.14 性能评估：在真实/模拟数据库上评估新增流程的性能开销，必要时引入缓存或并行优化手段

## 2. Tests
- [ ] 2.1 单测：Source/Sink/补边抽取模块在多种 QL 写法下的解析准确性（含注释、换行、不同 Config 结构）
- [ ] 2.2 单测：断流检测骨架生成与占位替换正确性，覆盖缺省/空补边、语言差异与转义场景
- [ ] 2.3 单测：补边规则生成器对赋值/属性/集合/调用/返回等典型断流点输出期望子句，含去重与限额逻辑
- [ ] 2.4 集成：在沙箱数据库中模拟“检测→补边→重跑”全流程，验证迭代终止条件与降级路径
- [ ] 2.5 回归：验证原有空结果检测与成功路径输出未受影响，必要时对 `CodeQLExecutionService` 与 Compose 层做冒烟测试
- [ ] 2.6 性能：统计新增流程在典型数据库上的耗时与 CLI 调用次数，确立基线并写入测试/文档

## 3. Docs
- [ ] 3.1 开发文档：说明检测流程、模块边界、算法规则、配置开关及扩展点
- [ ] 3.2 运维文档：记录默认配置、调优建议、性能注意事项、遥测字段说明
- [ ] 3.3 变更日志/发布说明：总结新能力、默认行为与潜在影响，指导用户灰度启用


