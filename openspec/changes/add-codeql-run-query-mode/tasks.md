# Tasks for add-codeql-run-query-mode

- [x] 需求确认与范围冻结（不影响既有 analyze→SARIF 流程，新增 run 模式并返回文本结果与路径）
- [x] API 设计：在 `CodeQLComposeTool._arun(...)` 新增 `exec_mode: {'analyze','run'}`，默认 `'analyze'`，返回字符串结构保持兼容
- [x] 工具实现：在 `utils/codeql.py` 新增 `run_query_and_decode_to_text(...)`（`query run` + `bqrs decode`）
  - [x] 生成临时 `.ql` 与 `qlpack.yml`
  - [x] 执行 `codeql query run` 输出 `.bqrs`
  - [x] 执行 `codeql bqrs decode --format=table`（或其他纯文本），捕获输出
  - [x] 将文本写入 `./temp/search_temp/query_<timestamp>.txt`，返回 `success/output/result_file`
  - [x] 处理目录创建、Windows 路径、超时与错误
- [x] 在 `tools/codeql_compose.py` 中根据 `exec_mode` 分支：
  - [x] `'analyze'`：沿用原逻辑，`execute_codeql_query(...)`（保持不变）
  - [x] `'run'`：调用 `run_query_and_decode_to_text(...)`，拼装返回：结果片段 + 文件路径
  - [x] 控制返回中结果正文长度，避免超长输出
- [x] 配置项（可选，若需要）：结果输出目录、超时、解码格式；默认无需改动 `config.py`
- [ ] 基础测试：
  - [ ] analyze 模式最小样例：生成 `output/result_*.sarif`，JSON 自动转换仍可用
  - [ ] run 模式最小样例：生成 `temp/search_temp/query_*.txt`，内容可读
  - [ ] 无结果/错误路径：返回明确可读信息
- [ ] 文档与示例：在 README 或示例中说明 `exec_mode` 的用法
- [ ] 运行 `openspec validate add-codeql-run-query-mode --strict`（如可用）并修复
- [ ] 提交评审，等待批准后实施合并
