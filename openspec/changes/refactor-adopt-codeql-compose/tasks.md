## 1. Implementation
- [x] 1.1 枚举并确认所有“生成 QL”调用点（不改代码）
  - `agents/python_source_analysis_agent.py: PythonSourceAnalysisAgent.generate_source_codeql_query`
  - `agents/c_source_analysis_agent.py: CSourceAnalysisAgent.generate_source_codeql_query`
  - `Analyze.py: run_multi_agent_analysis` 中 “CodeQL Query Generation” 阶段（`CodeQLGeneratorAgent.generate_codeql` 调用）
  - `GenerateCodeQL.py: generate_codeql_query` 主流程（`CodeQLGeneratorAgent`）
- [x] 1.2 为各调用点设计迁移到 `tools/CodeQLComposeTool` 的适配方式（不改工具）
  - 统一通过 `CodeQLComposeTool(analyzer, database_path, language, max_rounds)` 构造
  - 仅调用 `await tool._arun(requirement)`，其余执行/分析继续复用现有 Agent/Tool
  - 结果抽取遵循 ` ```ql ... ``` ` 或 `<codeql>...</codeql>` 提取
- [x] 1.3 在 `Analyze.py` 中用 `CodeQLComposeTool` 替换 `CodeQLGeneratorAgent` 生成步骤
  - 复用现有的 `MultiAgentAnalyzer` 实例
  - 传入来自 CLI/Case 的 `db_path` 与目标 `language="java"`
- [x] 1.4 在 `GenerateCodeQL.py` 中用 `CodeQLComposeTool` 统一生成
  - 复用 `MultiAgentAnalyzer`
  - 若使用 RAG，仅影响提示词生成，最终仍通过 `CodeQLComposeTool._arun()` 完成生成/验证
- [x] 1.5 在 `agents/python_source_analysis_agent.py` 中改用 `CodeQLComposeTool`
  - 初始化：`CodeQLComposeTool(analyzer=self.analyzer, database_path=self.database_path, language="python")`
  - 生成：`await tool._arun(requirement)`
- [x] 1.6 在 `agents/c_source_analysis_agent.py` 中改用 `CodeQLComposeTool`
  - 初始化：`CodeQLComposeTool(analyzer=self.analyzer, database_path=self.database_path, language="cpp")`
  - 生成：`await tool._arun(requirement)`
- [x] 1.7 清理无效引用
  - 移除对缺失模块 `tools.codeql_generator_tool` 的 import 与使用
  - 保留（或后续合并） `CodeQLRunnerAgent`，以便与 Compose 的执行解耦
- [x] 1.8 测试与示例同步
  - 更新 `tests/test_langchain_tools.py`：去除对 `CodeQLGeneratorTool` 的断言；增加 `CodeQLComposeTool` 属性与入参校验
  - 验证 `tests/test_codeql_compose_tool.py` 仍然通过
- [x] 1.9 文档与 README
  - `tools/README.md` 增补 `CodeQLComposeTool` 为统一生成入口

## 2. Validation
- [ ] 2.1 运行 `openspec validate refactor-adopt-codeql-compose --strict`
- [ ] 2.2 在不改动 `tools/codeql_compose.py` 的前提下，跑通示例数据库（本地/CI）
- [ ] 2.3 确认 Analyze 主流程与 Source Agents 对接正常（生成→执行→分析）
